#!/bin/bash
set +x
rm -rf app
mkdir app
mkdir app/src
uglifyjs index.js -c -m -o app/index.js
for MODULE in apiserver certs config db kube log main prune router util
do
    uglifyjs src/$MODULE.js --toplevel -c -m -o app/src/$MODULE.js
done
